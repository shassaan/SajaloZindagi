@model List<SajaloZindagi.Models.Services>
@{
    Layout = "_UserLayout";
}
<style>
    [data-notify="progressbar"] {
        margin-bottom: 0px;
        position: absolute;
        bottom: 0px;
        left: 0px;
        width: 100%;
        height: 5px;
    }
</style>
<br /><br /><br />
@if (Model != null)
{

    <br /><br />
    <div class="row">
        @for (int i = 0; i < Model.Count; i++)
        { 
            if (true)
            {

            }
            if (i > 0 && i % 3 == 0)
            {
            @:</div><br /><div class="row">
}


                                  <div class="col-md-4">
                                     
                                      <div class="well">
                                          <img src = "@Model[i].SDp" height = "200" width = "200" class="rounded-circle"/>

                                          <h4>@Model[i].SName</h4>
                                          <h3>@Model[i].SPrice</h3>
                                          <p>@Model[i].SDetail</p>
                                          <a href="/User/showImages/@Model[i].SId" class="m btn btn-primary">Show Images</a>
                                      </div>
                                      

                                  </div>
                              

}
    </div>
}
else
{
    <div class="panel panel-footer">NO services</div>
}


  